{
  "name": "statuta-monorepo",
  "private": true,
  "scripts": {
    "dev": "npm --prefix demo/frontend run dev",
    "test": "tsx engine/tests/run-tests.ts",
    "ci": "npm run build:evidence && npm run test",
    "build:evidence": "tsx scripts/build-evidence.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.3"
  }
}
